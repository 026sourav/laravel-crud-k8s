apiVersion: v1
kind: PersistentVolume
metadata:
  name: crud-pv
  name: laravel-crud
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: /mnt/data/mysql
