apiVersion: v1
kind: Service
metadata:
  name: crud-service
  namespace: laravel-crud
spec:
  type: NodePort
  selector:
    app: laravel-crud
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008
