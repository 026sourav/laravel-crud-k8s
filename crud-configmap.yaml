apiVersion: v1
kind: ConfigMap
metadata:
  name: crud-config
  namespace: laravel-crud
data:
  DB_CONNECTION: mysql
  DB_HOST: mysql-service
  DB_PORT: "3306"
  DB_DATABASE: laravel_db
  DB_USERNAME: root
